
// * make a function with variable of (renderMovies) and takes a parameter of (movieArray)
const renderMovies = 
// * write a .map() loop for (movieArray)
// * while the function loops, the picked movie should be called (currentMovie)
// * take the result of the (currentMovie) and save it to the variable of (movieHTMLArray)


// todo ! make a function that .map() the array of all the movie details of data.js
// todo ! variable used should be (movieArray)(renderMovies*function name)(currentMovie)(movieHTMLArray)